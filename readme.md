```
    && wget -O install.sh http://download.bt.cn/install/install-ubuntu_6.0.sh \
    && echo y|bash install.sh
```

```
docker run --name test -d test /bin/bash

docker container run -it test /bin/bash
```